
test_colors() {
	echo "$fg[black]test$reset_color"
	echo "$fg[red]test$reset_color"
	echo "$fg[green]test$reset_color"
	echo "$fg[yellow]test$reset_color"
	echo "$fg[blue]test$reset_color"
	echo "$fg[magenta]test$reset_color"
	echo "$fg[cyan]test$reset_color"
	echo "$fg[white]test$reset_color"
}
